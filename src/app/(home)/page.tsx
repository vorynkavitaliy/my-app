import ChatContainer from "@/entities/chat/chat.container";

const HomePage = () => {
  return (
    <div className="h-screen flex flex-col">
      <ChatContainer />
    </div>
  );
};

export default HomePage;
